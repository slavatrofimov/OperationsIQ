/**
 * Provisioning stub for `src/lib/rayfinClient.ts` (the Rayfin GraphQL client).
 * Provisioning reads tool metadata only; no network client is needed.
 * Mirrors the mock in `registryTools.test.ts`.
 */
export const client = {} as unknown;
export const getFabricAccountId = (): string => '';
export const getFabricAccountEmail = (): string => '';
